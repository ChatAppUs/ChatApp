{
  "name": "chatapp-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "ChatApp desktop client (Tauri / Rust shell around the web app)",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0"
  }
}
